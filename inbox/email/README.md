# Email Inbox

Imported email markdown records land here.

Account-specific imports may use subfolders such as `gmail-travel/`.

Google OAuth guest imports use scoped folders such as `user-<id>/`. Imported email is private and should remain ignored by Git except for this README.
