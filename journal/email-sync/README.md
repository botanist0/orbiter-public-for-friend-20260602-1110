# Email Sync Journal

Email sync runs should write logs here when they import messages, skip duplicates, or hit recoverable errors.

These logs can reveal account IDs, subjects, message IDs, or errors from private mailboxes. Keep this folder ignored by Git except for this README.
